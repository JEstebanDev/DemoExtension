// Background service worker para la extensión
// Por ahora está vacío pero puede ser útil para futuras funcionalidades

chrome.runtime.onInstalled.addListener(() => {
    console.log('Extensión Excel Stepper Form Filler instalada');
});

